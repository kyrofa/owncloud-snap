diff -rc snap_install_old/bin/apachectl snap_install/bin/apachectl
*** snap_install_old/bin/apachectl	2016-01-14 14:07:33.450452957 +0000
--- snap_install/bin/apachectl	2016-01-14 14:07:53.962452677 +0000
***************
*** 42,52 ****
  # --------------------                              --------------------
  # 
  # the path to your httpd binary, including options if necessary
! HTTPD='/CUSTOM_PREFIX/bin/httpd'
  #
  # pick up any necessary environment variables
! if test -f /CUSTOM_PREFIX/bin/envvars; then
!   . /CUSTOM_PREFIX/bin/envvars
  fi
  #
  # a command that outputs a formatted text version of the HTML at the
--- 42,52 ----
  # --------------------                              --------------------
  # 
  # the path to your httpd binary, including options if necessary
! HTTPD=${SNAP}/bin/httpd
  #
  # pick up any necessary environment variables
! if test -f ${SNAP}/bin/envvars; then
!   . ${SNAP}/bin/envvars
  fi
  #
  # a command that outputs a formatted text version of the HTML at the
***************
*** 78,84 ****
  
  case $ACMD in
  start|stop|restart|graceful|graceful-stop)
!     $HTTPD -k $ARGV
      ERROR=$?
      ;;
  startssl|sslstart|start-SSL)
--- 78,84 ----
  
  case $ACMD in
  start|stop|restart|graceful|graceful-stop)
!     $HTTPD -d ${SNAP} -k $ARGV
      ERROR=$?
      ;;
  startssl|sslstart|start-SSL)
***************
*** 88,94 ****
      ERROR=2
      ;;
  configtest)
!     $HTTPD -t
      ERROR=$?
      ;;
  status)
--- 88,94 ----
      ERROR=2
      ;;
  configtest)
!     $HTTPD -d ${SNAP} -t
      ERROR=$?
      ;;
  status)
***************
*** 98,104 ****
      $LYNX $STATUSURL
      ;;
  *)
!     $HTTPD "$@"
      ERROR=$?
  esac
  
--- 98,104 ----
      $LYNX $STATUSURL
      ;;
  *)
!     $HTTPD -d ${SNAP} "$@"
      ERROR=$?
  esac
  
diff -rc snap_install_old/bin/envvars snap_install/bin/envvars
*** snap_install_old/bin/envvars	2016-01-14 14:07:33.450452957 +0000
--- snap_install/bin/envvars	2016-01-14 14:07:53.962452677 +0000
***************
*** 19,27 ****
  # This file is generated from envvars-std.in
  #
  if test "x$LD_LIBRARY_PATH" != "x" ; then
!   LD_LIBRARY_PATH="/CUSTOM_PREFIX/lib:$LD_LIBRARY_PATH"
  else
!   LD_LIBRARY_PATH="/CUSTOM_PREFIX/lib"
  fi
  export LD_LIBRARY_PATH
  #
--- 19,27 ----
  # This file is generated from envvars-std.in
  #
  if test "x$LD_LIBRARY_PATH" != "x" ; then
!   LD_LIBRARY_PATH="${SNAP}/lib:$LD_LIBRARY_PATH"
  else
!   LD_LIBRARY_PATH="${SNAP}/lib"
  fi
  export LD_LIBRARY_PATH
  #
diff -rc snap_install_old/bin/envvars-std snap_install/bin/envvars-std
*** snap_install_old/bin/envvars-std	2016-01-14 14:07:33.450452957 +0000
--- snap_install/bin/envvars-std	2016-01-14 14:07:53.962452677 +0000
***************
*** 19,27 ****
  # This file is generated from envvars-std.in
  #
  if test "x$LD_LIBRARY_PATH" != "x" ; then
!   LD_LIBRARY_PATH="/CUSTOM_PREFIX/lib:$LD_LIBRARY_PATH"
  else
!   LD_LIBRARY_PATH="/CUSTOM_PREFIX/lib"
  fi
  export LD_LIBRARY_PATH
  #
--- 19,27 ----
  # This file is generated from envvars-std.in
  #
  if test "x$LD_LIBRARY_PATH" != "x" ; then
!   LD_LIBRARY_PATH="${SNAP}/lib:$LD_LIBRARY_PATH"
  else
!   LD_LIBRARY_PATH="${SNAP}/lib"
  fi
  export LD_LIBRARY_PATH
  #
diff -rc snap_install_old/conf/extra/httpd-autoindex.conf snap_install/conf/extra/httpd-autoindex.conf
*** snap_install_old/conf/extra/httpd-autoindex.conf	2016-01-14 14:07:33.458452957 +0000
--- snap_install/conf/extra/httpd-autoindex.conf	2016-01-14 14:07:53.962452677 +0000
***************
*** 18,26 ****
  # We include the /icons/ alias for FancyIndexed directory listings.  If
  # you do not use FancyIndexing, you may comment this out.
  #
! Alias /icons/ "/CUSTOM_PREFIX/icons/"
  
! <Directory "/CUSTOM_PREFIX/icons">
      Options Indexes MultiViews
      AllowOverride None
      Require all granted
--- 18,26 ----
  # We include the /icons/ alias for FancyIndexed directory listings.  If
  # you do not use FancyIndexing, you may comment this out.
  #
! Alias /icons/ "${SNAP}/icons/"
  
! <Directory "${SNAP}/icons">
      Options Indexes MultiViews
      AllowOverride None
      Require all granted
diff -rc snap_install_old/conf/extra/httpd-dav.conf snap_install/conf/extra/httpd-dav.conf
*** snap_install_old/conf/extra/httpd-dav.conf	2016-01-14 14:07:33.458452957 +0000
--- snap_install/conf/extra/httpd-dav.conf	2016-01-14 14:07:53.962452677 +0000
***************
*** 12,29 ****
  # on the directory where the DavLockDB is placed and on any directory where
  # "Dav On" is specified.
  
! DavLockDB "/CUSTOM_PREFIX/var/DavLock"
  
! Alias /uploads "/CUSTOM_PREFIX/uploads"
  
! <Directory "/CUSTOM_PREFIX/uploads">
      Dav On
  
      AuthType Digest
      AuthName DAV-upload
      # You can use the htdigest program to create the password database:
!     #   htdigest -c "/CUSTOM_PREFIX/user.passwd" DAV-upload admin
!     AuthUserFile "/CUSTOM_PREFIX/user.passwd"
      AuthDigestProvider file
  
      # Allow universal read-access, but writes are restricted
--- 12,29 ----
  # on the directory where the DavLockDB is placed and on any directory where
  # "Dav On" is specified.
  
! DavLockDB "${SNAP}/var/DavLock"
  
! Alias /uploads "${SNAP}/uploads"
  
! <Directory "${SNAP}/uploads">
      Dav On
  
      AuthType Digest
      AuthName DAV-upload
      # You can use the htdigest program to create the password database:
!     #   htdigest -c "${SNAP}/user.passwd" DAV-upload admin
!     AuthUserFile "${SNAP}/user.passwd"
      AuthDigestProvider file
  
      # Allow universal read-access, but writes are restricted
diff -rc snap_install_old/conf/extra/httpd-manual.conf snap_install/conf/extra/httpd-manual.conf
*** snap_install_old/conf/extra/httpd-manual.conf	2016-01-14 14:07:33.458452957 +0000
--- snap_install/conf/extra/httpd-manual.conf	2016-01-14 14:07:53.962452677 +0000
***************
*** 8,16 ****
  #                   mod_setenvif, mod_negotiation
  #
  
! AliasMatch ^/manual(?:/(?:da|de|en|es|fr|ja|ko|pt-br|ru|tr|zh-cn))?(/.*)?$ "/CUSTOM_PREFIX/manual$1"
  
! <Directory "/CUSTOM_PREFIX/manual">
      Options Indexes
      AllowOverride None
      Require all granted
--- 8,16 ----
  #                   mod_setenvif, mod_negotiation
  #
  
! AliasMatch ^/manual(?:/(?:da|de|en|es|fr|ja|ko|pt-br|ru|tr|zh-cn))?(/.*)?$ "${SNAP}/manual$1"
  
! <Directory "${SNAP}/manual">
      Options Indexes
      AllowOverride None
      Require all granted
diff -rc snap_install_old/conf/extra/httpd-mpm.conf snap_install/conf/extra/httpd-mpm.conf
*** snap_install_old/conf/extra/httpd-mpm.conf	2016-01-14 14:07:33.458452957 +0000
--- snap_install/conf/extra/httpd-mpm.conf	2016-01-14 14:07:53.966452677 +0000
***************
*** 9,15 ****
  # Note that this is the default PidFile for most MPMs.
  #
  <IfModule !mpm_netware_module>
!     PidFile "logs/httpd.pid"
  </IfModule>
  
  #
--- 9,15 ----
  # Note that this is the default PidFile for most MPMs.
  #
  <IfModule !mpm_netware_module>
!     PidFile "${SNAP_DATA}/apache/httpd.pid"
  </IfModule>
  
  #
diff -rc snap_install_old/conf/extra/httpd-multilang-errordoc.conf snap_install/conf/extra/httpd-multilang-errordoc.conf
*** snap_install_old/conf/extra/httpd-multilang-errordoc.conf	2016-01-14 14:07:33.458452957 +0000
--- snap_install/conf/extra/httpd-multilang-errordoc.conf	2016-01-14 14:07:53.966452677 +0000
***************
*** 15,28 ****
  #   Alias /error/include/ "/your/include/path/"
  #
  # which allows you to create your own set of files by starting with the
! # /CUSTOM_PREFIX/error/include/ files and copying them to /your/include/path/, 
  # even on a per-VirtualHost basis.  The default include files will display
  # your Apache version number and your ServerAdmin email address regardless
  # of the setting of ServerSignature.
  
! Alias /error/ "/CUSTOM_PREFIX/error/"
  
! <Directory "/CUSTOM_PREFIX/error">
      AllowOverride None
      Options IncludesNoExec
      AddOutputFilter Includes html
--- 15,28 ----
  #   Alias /error/include/ "/your/include/path/"
  #
  # which allows you to create your own set of files by starting with the
! # ${SNAP}/error/include/ files and copying them to /your/include/path/, 
  # even on a per-VirtualHost basis.  The default include files will display
  # your Apache version number and your ServerAdmin email address regardless
  # of the setting of ServerSignature.
  
! Alias /error/ "${SNAP}/error/"
  
! <Directory "${SNAP}/error">
      AllowOverride None
      Options IncludesNoExec
      AddOutputFilter Includes html
diff -rc snap_install_old/conf/extra/httpd-ssl.conf snap_install/conf/extra/httpd-ssl.conf
*** snap_install_old/conf/extra/httpd-ssl.conf	2016-01-14 14:07:33.458452957 +0000
--- snap_install/conf/extra/httpd-ssl.conf	2016-01-14 14:07:53.966452677 +0000
***************
*** 88,95 ****
  #   Inter-Process Session Cache:
  #   Configure the SSL Session Cache: First the mechanism 
  #   to use and second the expiring timeout (in seconds).
! #SSLSessionCache         "dbm:/CUSTOM_PREFIX/logs/ssl_scache"
! SSLSessionCache        "shmcb:/CUSTOM_PREFIX/logs/ssl_scache(512000)"
  SSLSessionCacheTimeout  300
  
  #   OCSP Stapling (requires OpenSSL 0.9.8h or later)
--- 88,95 ----
  #   Inter-Process Session Cache:
  #   Configure the SSL Session Cache: First the mechanism 
  #   to use and second the expiring timeout (in seconds).
! #SSLSessionCache         "dbm:${SNAP_DATA}/apache/ssl_scache"
! SSLSessionCache        "shmcb:${SNAP_DATA}/apache/ssl_scache(512000)"
  SSLSessionCacheTimeout  300
  
  #   OCSP Stapling (requires OpenSSL 0.9.8h or later)
***************
*** 106,112 ****
  #   the same mechanism that is used for the SSL session cache
  #   above.  If stapling is used with more than a few certificates,
  #   the size may need to be increased.  (AH01929 will be logged.)
! #SSLStaplingCache "shmcb:/CUSTOM_PREFIX/logs/ssl_stapling(32768)"
  
  #   Seconds before valid OCSP responses are expired from the cache
  #SSLStaplingStandardCacheTimeout 3600
--- 106,112 ----
  #   the same mechanism that is used for the SSL session cache
  #   above.  If stapling is used with more than a few certificates,
  #   the size may need to be increased.  (AH01929 will be logged.)
! #SSLStaplingCache "shmcb:${SNAP_DATA}/apache/ssl_stapling(32768)"
  
  #   Seconds before valid OCSP responses are expired from the cache
  #SSLStaplingStandardCacheTimeout 3600
***************
*** 121,131 ****
  <VirtualHost _default_:443>
  
  #   General setup for the virtual host
! DocumentRoot "/CUSTOM_PREFIX/htdocs"
  ServerName www.example.com:443
  ServerAdmin you@example.com
! ErrorLog "/CUSTOM_PREFIX/logs/error_log"
! TransferLog "/CUSTOM_PREFIX/logs/access_log"
  
  #   SSL Engine Switch:
  #   Enable/Disable SSL for this virtual host.
--- 121,131 ----
  <VirtualHost _default_:443>
  
  #   General setup for the virtual host
! DocumentRoot "${SNAP}/htdocs"
  ServerName www.example.com:443
  ServerAdmin you@example.com
! ErrorLog "${SNAP_DATA}/apache/error_log"
! TransferLog "${SNAP_DATA}/apache/access_log"
  
  #   SSL Engine Switch:
  #   Enable/Disable SSL for this virtual host.
***************
*** 141,149 ****
  #   Some ECC cipher suites (http://www.ietf.org/rfc/rfc4492.txt)
  #   require an ECC certificate which can also be configured in
  #   parallel.
! SSLCertificateFile "/CUSTOM_PREFIX/conf/server.crt"
! #SSLCertificateFile "/CUSTOM_PREFIX/conf/server-dsa.crt"
! #SSLCertificateFile "/CUSTOM_PREFIX/conf/server-ecc.crt"
  
  #   Server Private Key:
  #   If the key is not combined with the certificate, use this
--- 141,149 ----
  #   Some ECC cipher suites (http://www.ietf.org/rfc/rfc4492.txt)
  #   require an ECC certificate which can also be configured in
  #   parallel.
! SSLCertificateFile "${SNAP}/conf/server.crt"
! #SSLCertificateFile "${SNAP}/conf/server-dsa.crt"
! #SSLCertificateFile "${SNAP}/conf/server-ecc.crt"
  
  #   Server Private Key:
  #   If the key is not combined with the certificate, use this
***************
*** 151,159 ****
  #   you've both a RSA and a DSA private key you can configure
  #   both in parallel (to also allow the use of DSA ciphers, etc.)
  #   ECC keys, when in use, can also be configured in parallel
! SSLCertificateKeyFile "/CUSTOM_PREFIX/conf/server.key"
! #SSLCertificateKeyFile "/CUSTOM_PREFIX/conf/server-dsa.key"
! #SSLCertificateKeyFile "/CUSTOM_PREFIX/conf/server-ecc.key"
  
  #   Server Certificate Chain:
  #   Point SSLCertificateChainFile at a file containing the
--- 151,159 ----
  #   you've both a RSA and a DSA private key you can configure
  #   both in parallel (to also allow the use of DSA ciphers, etc.)
  #   ECC keys, when in use, can also be configured in parallel
! SSLCertificateKeyFile "${SNAP}/conf/server.key"
! #SSLCertificateKeyFile "${SNAP}/conf/server-dsa.key"
! #SSLCertificateKeyFile "${SNAP}/conf/server-ecc.key"
  
  #   Server Certificate Chain:
  #   Point SSLCertificateChainFile at a file containing the
***************
*** 162,168 ****
  #   the referenced file can be the same as SSLCertificateFile
  #   when the CA certificates are directly appended to the server
  #   certificate for convenience.
! #SSLCertificateChainFile "/CUSTOM_PREFIX/conf/server-ca.crt"
  
  #   Certificate Authority (CA):
  #   Set the CA certificate verification path where to find CA
--- 162,168 ----
  #   the referenced file can be the same as SSLCertificateFile
  #   when the CA certificates are directly appended to the server
  #   certificate for convenience.
! #SSLCertificateChainFile "${SNAP}/conf/server-ca.crt"
  
  #   Certificate Authority (CA):
  #   Set the CA certificate verification path where to find CA
***************
*** 171,178 ****
  #   Note: Inside SSLCACertificatePath you need hash symlinks
  #         to point to the certificate files. Use the provided
  #         Makefile to update the hash symlinks after changes.
! #SSLCACertificatePath "/CUSTOM_PREFIX/conf/ssl.crt"
! #SSLCACertificateFile "/CUSTOM_PREFIX/conf/ssl.crt/ca-bundle.crt"
  
  #   Certificate Revocation Lists (CRL):
  #   Set the CA revocation path where to find CA CRLs for client
--- 171,178 ----
  #   Note: Inside SSLCACertificatePath you need hash symlinks
  #         to point to the certificate files. Use the provided
  #         Makefile to update the hash symlinks after changes.
! #SSLCACertificatePath "${SNAP}/conf/ssl.crt"
! #SSLCACertificateFile "${SNAP}/conf/ssl.crt/ca-bundle.crt"
  
  #   Certificate Revocation Lists (CRL):
  #   Set the CA revocation path where to find CA CRLs for client
***************
*** 183,190 ****
  #   Note: Inside SSLCARevocationPath you need hash symlinks
  #         to point to the certificate files. Use the provided
  #         Makefile to update the hash symlinks after changes.
! #SSLCARevocationPath "/CUSTOM_PREFIX/conf/ssl.crl"
! #SSLCARevocationFile "/CUSTOM_PREFIX/conf/ssl.crl/ca-bundle.crl"
  #SSLCARevocationCheck chain
  
  #   Client Authentication (Type):
--- 183,190 ----
  #   Note: Inside SSLCARevocationPath you need hash symlinks
  #         to point to the certificate files. Use the provided
  #         Makefile to update the hash symlinks after changes.
! #SSLCARevocationPath "${SNAP}/conf/ssl.crl"
! #SSLCARevocationFile "${SNAP}/conf/ssl.crl/ca-bundle.crl"
  #SSLCARevocationCheck chain
  
  #   Client Authentication (Type):
***************
*** 200,207 ****
  #   file (containing login information for SRP user accounts). 
  #   Requires OpenSSL 1.0.1 or newer. See the mod_ssl FAQ for
  #   detailed instructions on creating this file. Example:
! #   "openssl srp -srpvfile /CUSTOM_PREFIX/conf/passwd.srpv -add username"
! #SSLSRPVerifierFile "/CUSTOM_PREFIX/conf/passwd.srpv"
  
  #   Access Control:
  #   With SSLRequire you can do per-directory access control based
--- 200,207 ----
  #   file (containing login information for SRP user accounts). 
  #   Requires OpenSSL 1.0.1 or newer. See the mod_ssl FAQ for
  #   detailed instructions on creating this file. Example:
! #   "openssl srp -srpvfile ${SNAP}/conf/passwd.srpv -add username"
! #SSLSRPVerifierFile "${SNAP}/conf/passwd.srpv"
  
  #   Access Control:
  #   With SSLRequire you can do per-directory access control based
***************
*** 249,255 ****
  <FilesMatch "\.(cgi|shtml|phtml|php)$">
      SSLOptions +StdEnvVars
  </FilesMatch>
! <Directory "/CUSTOM_PREFIX/cgi-bin">
      SSLOptions +StdEnvVars
  </Directory>
  
--- 249,255 ----
  <FilesMatch "\.(cgi|shtml|phtml|php)$">
      SSLOptions +StdEnvVars
  </FilesMatch>
! <Directory "${SNAP}/cgi-bin">
      SSLOptions +StdEnvVars
  </Directory>
  
***************
*** 284,290 ****
  #   Per-Server Logging:
  #   The home of a custom SSL log file. Use this when you want a
  #   compact non-error SSL logfile on a virtual host basis.
! CustomLog "/CUSTOM_PREFIX/logs/ssl_request_log" \
            "%t %h %{SSL_PROTOCOL}x %{SSL_CIPHER}x \"%r\" %b"
  
  </VirtualHost>                                  
--- 284,290 ----
  #   Per-Server Logging:
  #   The home of a custom SSL log file. Use this when you want a
  #   compact non-error SSL logfile on a virtual host basis.
! CustomLog "${SNAP_DATA}/apache/ssl_request_log" \
            "%t %h %{SSL_PROTOCOL}x %{SSL_CIPHER}x \"%r\" %b"
  
  </VirtualHost>                                  
diff -rc snap_install_old/conf/extra/httpd-vhosts.conf snap_install/conf/extra/httpd-vhosts.conf
*** snap_install_old/conf/extra/httpd-vhosts.conf	2016-01-14 14:07:33.458452957 +0000
--- snap_install/conf/extra/httpd-vhosts.conf	2016-01-14 14:07:53.966452677 +0000
***************
*** 22,40 ****
  #
  <VirtualHost *:80>
      ServerAdmin webmaster@dummy-host.example.com
!     DocumentRoot "/CUSTOM_PREFIX/docs/dummy-host.example.com"
      ServerName dummy-host.example.com
      ServerAlias www.dummy-host.example.com
!     ErrorLog "logs/dummy-host.example.com-error_log"
!     CustomLog "logs/dummy-host.example.com-access_log" common
  </VirtualHost>
  
  <VirtualHost *:80>
      ServerAdmin webmaster@dummy-host2.example.com
!     DocumentRoot "/CUSTOM_PREFIX/docs/dummy-host2.example.com"
      ServerName dummy-host2.example.com
!     ErrorLog "logs/dummy-host2.example.com-error_log"
!     CustomLog "logs/dummy-host2.example.com-access_log" common
  </VirtualHost>
  
  
--- 22,40 ----
  #
  <VirtualHost *:80>
      ServerAdmin webmaster@dummy-host.example.com
!     DocumentRoot "${SNAP}/docs/dummy-host.example.com"
      ServerName dummy-host.example.com
      ServerAlias www.dummy-host.example.com
!     ErrorLog "${SNAP_DATA}/apache/dummy-host.example.com-error_log"
!     CustomLog "${SNAP_DATA}/apache/dummy-host.example.com-access_log" common
  </VirtualHost>
  
  <VirtualHost *:80>
      ServerAdmin webmaster@dummy-host2.example.com
!     DocumentRoot "${SNAP}/docs/dummy-host2.example.com"
      ServerName dummy-host2.example.com
!     ErrorLog "${SNAP_DATA}/apache/dummy-host2.example.com-error_log"
!     CustomLog "${SNAP_DATA}/apache/dummy-host2.example.com-access_log" common
  </VirtualHost>
  
  
diff -rc snap_install_old/conf/httpd.conf snap_install/conf/httpd.conf
*** snap_install_old/conf/httpd.conf	2016-01-14 14:07:33.458452957 +0000
--- snap_install/conf/httpd.conf	2016-01-14 14:09:25.498451428 +0000
***************
*** 28,34 ****
  # same ServerRoot for multiple httpd daemons, you will need to change at
  # least PidFile.
  #
! ServerRoot "/CUSTOM_PREFIX"
  
  #
  # Mutex: Allows you to set the mutex mechanism and mutex file directory
--- 28,49 ----
  # same ServerRoot for multiple httpd daemons, you will need to change at
  # least PidFile.
  #
! ServerRoot "${SNAP}"
! 
! PidFile "${SNAP_DATA}/apache/httpd.pid"
! 
! <FilesMatch \.php$>
!    SetHandler application/x-httpd-php
! </FilesMatch>
! 
! PHPIniDir "${SNAP}/lib/php.ini"
! 
! # Serve static assets for apps in a writable location.
! Alias "/apps" "${SNAP_DATA}/owncloud/apps"
! <Directory "${SNAP_DATA}/owncloud/apps">
! 	AllowOverride None
! 	Require all granted
! </Directory>
  
  #
  # Mutex: Allows you to set the mutex mechanism and mutex file directory
***************
*** 38,44 ****
  # mutex file directory is not on a local disk or is not appropriate for some
  # other reason.
  #
! # Mutex default:logs

  #
  # Listen: Allows you to bind Apache to specific IP addresses and/or
--- 38,47 ----
  # mutex file directory is not on a local disk or is not appropriate for some
  # other reason.
  #
! # Using pthread here, since Apache tries to chown the file-based mutex which
! # isn't allowed in Snappy, and Ubuntu supports robust pthread mutexes that
! # can be recovered if the child process terminates abnormally.
! Mutex pthread

  #
  # Listen: Allows you to bind Apache to specific IP addresses and/or
***************
*** 157,164 ****
  # It is usually good practice to create a dedicated user and group for
  # running httpd, as with most system services.
  #
! User daemon
! Group daemon
  
  </IfModule>
  
--- 165,172 ----
  # It is usually good practice to create a dedicated user and group for
  # running httpd, as with most system services.
  #
! #User daemon
! #Group daemon
  
  </IfModule>
  
***************
*** 212,219 ****
  # documents. By default, all requests are taken from this directory, but
  # symbolic links and aliases may be used to point to other locations.
  #
! DocumentRoot "/CUSTOM_PREFIX/htdocs"
! <Directory "/CUSTOM_PREFIX/htdocs">
      #
      # Possible values for the Options directive are "None", "All",
      # or any combination of:
--- 220,227 ----
  # documents. By default, all requests are taken from this directory, but
  # symbolic links and aliases may be used to point to other locations.
  #
! DocumentRoot "${SNAP}/htdocs"
! <Directory "${SNAP}/htdocs">
      #
      # Possible values for the Options directive are "None", "All",
      # or any combination of:
***************
*** 226,232 ****
      # http://httpd.apache.org/docs/2.4/mod/core.html#options
      # for more information.
      #
!     Options Indexes FollowSymLinks
  
      #
      # AllowOverride controls what directives may be placed in .htaccess files.
--- 234,240 ----
      # http://httpd.apache.org/docs/2.4/mod/core.html#options
      # for more information.
      #
!     Options FollowSymLinks
  
      #
      # AllowOverride controls what directives may be placed in .htaccess files.
***************
*** 246,252 ****
  # is requested.
  #
  <IfModule dir_module>
!     DirectoryIndex index.html
  </IfModule>
  
  #
--- 254,260 ----
  # is requested.
  #
  <IfModule dir_module>
!     DirectoryIndex index.html index.php
  </IfModule>
  
  #
***************
*** 264,270 ****
  # logged here.  If you *do* define an error logfile for a <VirtualHost>
  # container, that host's errors will be logged there and not here.
  #
! ErrorLog "logs/error_log"
  
  #
  # LogLevel: Control the number of messages logged to the error_log.
--- 272,278 ----
  # logged here.  If you *do* define an error logfile for a <VirtualHost>
  # container, that host's errors will be logged there and not here.
  #
! ErrorLog "${SNAP_DATA}/apache/error_log"
  
  #
  # LogLevel: Control the number of messages logged to the error_log.
***************
*** 293,305 ****
      # define per-<VirtualHost> access logfiles, transactions will be
      # logged therein and *not* in this file.
      #
!     CustomLog "logs/access_log" common
  
      #
      # If you prefer a logfile with access, agent, and referer information
      # (Combined Logfile Format) you can use the following directive.
      #
!     #CustomLog "logs/access_log" combined
  </IfModule>
  
  <IfModule alias_module>
--- 301,313 ----
      # define per-<VirtualHost> access logfiles, transactions will be
      # logged therein and *not* in this file.
      #
!     CustomLog "${SNAP_DATA}/apache/access_log" common
  
      #
      # If you prefer a logfile with access, agent, and referer information
      # (Combined Logfile Format) you can use the following directive.
      #
!     #CustomLog "${SNAP_DATA}/apache/access_log" combined
  </IfModule>
  
  <IfModule alias_module>
***************
*** 329,335 ****
      # client.  The same rules about trailing "/" apply to ScriptAlias
      # directives as to Alias.
      #
!     ScriptAlias /cgi-bin/ "/CUSTOM_PREFIX/cgi-bin/"
  
  </IfModule>
  
--- 337,343 ----
      # client.  The same rules about trailing "/" apply to ScriptAlias
      # directives as to Alias.
      #
!     ScriptAlias /cgi-bin/ "${SNAP}/cgi-bin/"
  
  </IfModule>
  
***************
*** 342,351 ****
  </IfModule>
  
  #
! # "/CUSTOM_PREFIX/cgi-bin" should be changed to whatever your ScriptAliased
  # CGI directory exists, if you have that configured.
  #
! <Directory "/CUSTOM_PREFIX/cgi-bin">
      AllowOverride None
      Options None
      Require all granted
--- 350,359 ----
  </IfModule>
  
  #
! # "${SNAP}/cgi-bin" should be changed to whatever your ScriptAliased
  # CGI directory exists, if you have that configured.
  #
! <Directory "${SNAP}/cgi-bin">
      AllowOverride None
      Options None
      Require all granted
diff -rc snap_install_old/conf/original/extra/httpd-autoindex.conf snap_install/conf/original/extra/httpd-autoindex.conf
*** snap_install_old/conf/original/extra/httpd-autoindex.conf	2016-01-14 14:07:33.458452957 +0000
--- snap_install/conf/original/extra/httpd-autoindex.conf	2016-01-14 14:07:53.966452677 +0000
***************
*** 18,26 ****
  # We include the /icons/ alias for FancyIndexed directory listings.  If
  # you do not use FancyIndexing, you may comment this out.
  #
! Alias /icons/ "/CUSTOM_PREFIX/icons/"
  
! <Directory "/CUSTOM_PREFIX/icons">
      Options Indexes MultiViews
      AllowOverride None
      Require all granted
--- 18,26 ----
  # We include the /icons/ alias for FancyIndexed directory listings.  If
  # you do not use FancyIndexing, you may comment this out.
  #
! Alias /icons/ "${SNAP}/icons/"
  
! <Directory "${SNAP}/icons">
      Options Indexes MultiViews
      AllowOverride None
      Require all granted
diff -rc snap_install_old/conf/original/extra/httpd-dav.conf snap_install/conf/original/extra/httpd-dav.conf
*** snap_install_old/conf/original/extra/httpd-dav.conf	2016-01-14 14:07:33.458452957 +0000
--- snap_install/conf/original/extra/httpd-dav.conf	2016-01-14 14:07:53.966452677 +0000
***************
*** 12,29 ****
  # on the directory where the DavLockDB is placed and on any directory where
  # "Dav On" is specified.
  
! DavLockDB "/CUSTOM_PREFIX/var/DavLock"
  
! Alias /uploads "/CUSTOM_PREFIX/uploads"
  
! <Directory "/CUSTOM_PREFIX/uploads">
      Dav On
  
      AuthType Digest
      AuthName DAV-upload
      # You can use the htdigest program to create the password database:
!     #   htdigest -c "/CUSTOM_PREFIX/user.passwd" DAV-upload admin
!     AuthUserFile "/CUSTOM_PREFIX/user.passwd"
      AuthDigestProvider file
  
      # Allow universal read-access, but writes are restricted
--- 12,29 ----
  # on the directory where the DavLockDB is placed and on any directory where
  # "Dav On" is specified.
  
! DavLockDB "${SNAP}/var/DavLock"
  
! Alias /uploads "${SNAP}/uploads"
  
! <Directory "${SNAP}/uploads">
      Dav On
  
      AuthType Digest
      AuthName DAV-upload
      # You can use the htdigest program to create the password database:
!     #   htdigest -c "${SNAP}/user.passwd" DAV-upload admin
!     AuthUserFile "${SNAP}/user.passwd"
      AuthDigestProvider file
  
      # Allow universal read-access, but writes are restricted
diff -rc snap_install_old/conf/original/extra/httpd-manual.conf snap_install/conf/original/extra/httpd-manual.conf
*** snap_install_old/conf/original/extra/httpd-manual.conf	2016-01-14 14:07:33.458452957 +0000
--- snap_install/conf/original/extra/httpd-manual.conf	2016-01-14 14:07:53.966452677 +0000
***************
*** 8,16 ****
  #                   mod_setenvif, mod_negotiation
  #
  
! AliasMatch ^/manual(?:/(?:da|de|en|es|fr|ja|ko|pt-br|ru|tr|zh-cn))?(/.*)?$ "/CUSTOM_PREFIX/manual$1"
  
! <Directory "/CUSTOM_PREFIX/manual">
      Options Indexes
      AllowOverride None
      Require all granted
--- 8,16 ----
  #                   mod_setenvif, mod_negotiation
  #
  
! AliasMatch ^/manual(?:/(?:da|de|en|es|fr|ja|ko|pt-br|ru|tr|zh-cn))?(/.*)?$ "${SNAP}/manual$1"
  
! <Directory "${SNAP}/manual">
      Options Indexes
      AllowOverride None
      Require all granted
diff -rc snap_install_old/conf/original/extra/httpd-multilang-errordoc.conf snap_install/conf/original/extra/httpd-multilang-errordoc.conf
*** snap_install_old/conf/original/extra/httpd-multilang-errordoc.conf	2016-01-14 14:07:33.462452957 +0000
--- snap_install/conf/original/extra/httpd-multilang-errordoc.conf	2016-01-14 14:07:53.966452677 +0000
***************
*** 15,28 ****
  #   Alias /error/include/ "/your/include/path/"
  #
  # which allows you to create your own set of files by starting with the
! # /CUSTOM_PREFIX/error/include/ files and copying them to /your/include/path/, 
  # even on a per-VirtualHost basis.  The default include files will display
  # your Apache version number and your ServerAdmin email address regardless
  # of the setting of ServerSignature.
  
! Alias /error/ "/CUSTOM_PREFIX/error/"
  
! <Directory "/CUSTOM_PREFIX/error">
      AllowOverride None
      Options IncludesNoExec
      AddOutputFilter Includes html
--- 15,28 ----
  #   Alias /error/include/ "/your/include/path/"
  #
  # which allows you to create your own set of files by starting with the
! # ${SNAP}/error/include/ files and copying them to /your/include/path/, 
  # even on a per-VirtualHost basis.  The default include files will display
  # your Apache version number and your ServerAdmin email address regardless
  # of the setting of ServerSignature.
  
! Alias /error/ "${SNAP}/error/"
  
! <Directory "${SNAP}/error">
      AllowOverride None
      Options IncludesNoExec
      AddOutputFilter Includes html
diff -rc snap_install_old/conf/original/extra/httpd-ssl.conf snap_install/conf/original/extra/httpd-ssl.conf
*** snap_install_old/conf/original/extra/httpd-ssl.conf	2016-01-14 14:07:33.462452957 +0000
--- snap_install/conf/original/extra/httpd-ssl.conf	2016-01-14 14:07:53.966452677 +0000
***************
*** 88,95 ****
  #   Inter-Process Session Cache:
  #   Configure the SSL Session Cache: First the mechanism 
  #   to use and second the expiring timeout (in seconds).
! #SSLSessionCache         "dbm:/CUSTOM_PREFIX/logs/ssl_scache"
! SSLSessionCache        "shmcb:/CUSTOM_PREFIX/logs/ssl_scache(512000)"
  SSLSessionCacheTimeout  300
  
  #   OCSP Stapling (requires OpenSSL 0.9.8h or later)
--- 88,95 ----
  #   Inter-Process Session Cache:
  #   Configure the SSL Session Cache: First the mechanism 
  #   to use and second the expiring timeout (in seconds).
! #SSLSessionCache         "dbm:${SNAP}/logs/ssl_scache"
! SSLSessionCache        "shmcb:${SNAP}/logs/ssl_scache(512000)"
  SSLSessionCacheTimeout  300
  
  #   OCSP Stapling (requires OpenSSL 0.9.8h or later)
***************
*** 106,112 ****
  #   the same mechanism that is used for the SSL session cache
  #   above.  If stapling is used with more than a few certificates,
  #   the size may need to be increased.  (AH01929 will be logged.)
! #SSLStaplingCache "shmcb:/CUSTOM_PREFIX/logs/ssl_stapling(32768)"
  
  #   Seconds before valid OCSP responses are expired from the cache
  #SSLStaplingStandardCacheTimeout 3600
--- 106,112 ----
  #   the same mechanism that is used for the SSL session cache
  #   above.  If stapling is used with more than a few certificates,
  #   the size may need to be increased.  (AH01929 will be logged.)
! #SSLStaplingCache "shmcb:${SNAP}/logs/ssl_stapling(32768)"
  
  #   Seconds before valid OCSP responses are expired from the cache
  #SSLStaplingStandardCacheTimeout 3600
***************
*** 121,131 ****
  <VirtualHost _default_:443>
  
  #   General setup for the virtual host
! DocumentRoot "/CUSTOM_PREFIX/htdocs"
  ServerName www.example.com:443
  ServerAdmin you@example.com
! ErrorLog "/CUSTOM_PREFIX/logs/error_log"
! TransferLog "/CUSTOM_PREFIX/logs/access_log"
  
  #   SSL Engine Switch:
  #   Enable/Disable SSL for this virtual host.
--- 121,131 ----
  <VirtualHost _default_:443>
  
  #   General setup for the virtual host
! DocumentRoot "${SNAP}/htdocs"
  ServerName www.example.com:443
  ServerAdmin you@example.com
! ErrorLog "${SNAP}/logs/error_log"
! TransferLog "${SNAP}/logs/access_log"
  
  #   SSL Engine Switch:
  #   Enable/Disable SSL for this virtual host.
***************
*** 141,149 ****
  #   Some ECC cipher suites (http://www.ietf.org/rfc/rfc4492.txt)
  #   require an ECC certificate which can also be configured in
  #   parallel.
! SSLCertificateFile "/CUSTOM_PREFIX/conf/server.crt"
! #SSLCertificateFile "/CUSTOM_PREFIX/conf/server-dsa.crt"
! #SSLCertificateFile "/CUSTOM_PREFIX/conf/server-ecc.crt"
  
  #   Server Private Key:
  #   If the key is not combined with the certificate, use this
--- 141,149 ----
  #   Some ECC cipher suites (http://www.ietf.org/rfc/rfc4492.txt)
  #   require an ECC certificate which can also be configured in
  #   parallel.
! SSLCertificateFile "${SNAP}/conf/server.crt"
! #SSLCertificateFile "${SNAP}/conf/server-dsa.crt"
! #SSLCertificateFile "${SNAP}/conf/server-ecc.crt"
  
  #   Server Private Key:
  #   If the key is not combined with the certificate, use this
***************
*** 151,159 ****
  #   you've both a RSA and a DSA private key you can configure
  #   both in parallel (to also allow the use of DSA ciphers, etc.)
  #   ECC keys, when in use, can also be configured in parallel
! SSLCertificateKeyFile "/CUSTOM_PREFIX/conf/server.key"
! #SSLCertificateKeyFile "/CUSTOM_PREFIX/conf/server-dsa.key"
! #SSLCertificateKeyFile "/CUSTOM_PREFIX/conf/server-ecc.key"
  
  #   Server Certificate Chain:
  #   Point SSLCertificateChainFile at a file containing the
--- 151,159 ----
  #   you've both a RSA and a DSA private key you can configure
  #   both in parallel (to also allow the use of DSA ciphers, etc.)
  #   ECC keys, when in use, can also be configured in parallel
! SSLCertificateKeyFile "${SNAP}/conf/server.key"
! #SSLCertificateKeyFile "${SNAP}/conf/server-dsa.key"
! #SSLCertificateKeyFile "${SNAP}/conf/server-ecc.key"
  
  #   Server Certificate Chain:
  #   Point SSLCertificateChainFile at a file containing the
***************
*** 162,168 ****
  #   the referenced file can be the same as SSLCertificateFile
  #   when the CA certificates are directly appended to the server
  #   certificate for convenience.
! #SSLCertificateChainFile "/CUSTOM_PREFIX/conf/server-ca.crt"
  
  #   Certificate Authority (CA):
  #   Set the CA certificate verification path where to find CA
--- 162,168 ----
  #   the referenced file can be the same as SSLCertificateFile
  #   when the CA certificates are directly appended to the server
  #   certificate for convenience.
! #SSLCertificateChainFile "${SNAP}/conf/server-ca.crt"
  
  #   Certificate Authority (CA):
  #   Set the CA certificate verification path where to find CA
***************
*** 171,178 ****
  #   Note: Inside SSLCACertificatePath you need hash symlinks
  #         to point to the certificate files. Use the provided
  #         Makefile to update the hash symlinks after changes.
! #SSLCACertificatePath "/CUSTOM_PREFIX/conf/ssl.crt"
! #SSLCACertificateFile "/CUSTOM_PREFIX/conf/ssl.crt/ca-bundle.crt"
  
  #   Certificate Revocation Lists (CRL):
  #   Set the CA revocation path where to find CA CRLs for client
--- 171,178 ----
  #   Note: Inside SSLCACertificatePath you need hash symlinks
  #         to point to the certificate files. Use the provided
  #         Makefile to update the hash symlinks after changes.
! #SSLCACertificatePath "${SNAP}/conf/ssl.crt"
! #SSLCACertificateFile "${SNAP}/conf/ssl.crt/ca-bundle.crt"
  
  #   Certificate Revocation Lists (CRL):
  #   Set the CA revocation path where to find CA CRLs for client
***************
*** 183,190 ****
  #   Note: Inside SSLCARevocationPath you need hash symlinks
  #         to point to the certificate files. Use the provided
  #         Makefile to update the hash symlinks after changes.
! #SSLCARevocationPath "/CUSTOM_PREFIX/conf/ssl.crl"
! #SSLCARevocationFile "/CUSTOM_PREFIX/conf/ssl.crl/ca-bundle.crl"
  #SSLCARevocationCheck chain
  
  #   Client Authentication (Type):
--- 183,190 ----
  #   Note: Inside SSLCARevocationPath you need hash symlinks
  #         to point to the certificate files. Use the provided
  #         Makefile to update the hash symlinks after changes.
! #SSLCARevocationPath "${SNAP}/conf/ssl.crl"
! #SSLCARevocationFile "${SNAP}/conf/ssl.crl/ca-bundle.crl"
  #SSLCARevocationCheck chain
  
  #   Client Authentication (Type):
***************
*** 200,207 ****
  #   file (containing login information for SRP user accounts). 
  #   Requires OpenSSL 1.0.1 or newer. See the mod_ssl FAQ for
  #   detailed instructions on creating this file. Example:
! #   "openssl srp -srpvfile /CUSTOM_PREFIX/conf/passwd.srpv -add username"
! #SSLSRPVerifierFile "/CUSTOM_PREFIX/conf/passwd.srpv"
  
  #   Access Control:
  #   With SSLRequire you can do per-directory access control based
--- 200,207 ----
  #   file (containing login information for SRP user accounts). 
  #   Requires OpenSSL 1.0.1 or newer. See the mod_ssl FAQ for
  #   detailed instructions on creating this file. Example:
! #   "openssl srp -srpvfile ${SNAP}/conf/passwd.srpv -add username"
! #SSLSRPVerifierFile "${SNAP}/conf/passwd.srpv"
  
  #   Access Control:
  #   With SSLRequire you can do per-directory access control based
***************
*** 249,255 ****
  <FilesMatch "\.(cgi|shtml|phtml|php)$">
      SSLOptions +StdEnvVars
  </FilesMatch>
! <Directory "/CUSTOM_PREFIX/cgi-bin">
      SSLOptions +StdEnvVars
  </Directory>
  
--- 249,255 ----
  <FilesMatch "\.(cgi|shtml|phtml|php)$">
      SSLOptions +StdEnvVars
  </FilesMatch>
! <Directory "${SNAP}/cgi-bin">
      SSLOptions +StdEnvVars
  </Directory>
  
***************
*** 284,290 ****
  #   Per-Server Logging:
  #   The home of a custom SSL log file. Use this when you want a
  #   compact non-error SSL logfile on a virtual host basis.
! CustomLog "/CUSTOM_PREFIX/logs/ssl_request_log" \
            "%t %h %{SSL_PROTOCOL}x %{SSL_CIPHER}x \"%r\" %b"
  
  </VirtualHost>                                  
--- 284,290 ----
  #   Per-Server Logging:
  #   The home of a custom SSL log file. Use this when you want a
  #   compact non-error SSL logfile on a virtual host basis.
! CustomLog "${SNAP}/logs/ssl_request_log" \
            "%t %h %{SSL_PROTOCOL}x %{SSL_CIPHER}x \"%r\" %b"
  
  </VirtualHost>                                  
diff -rc snap_install_old/conf/original/extra/httpd-vhosts.conf snap_install/conf/original/extra/httpd-vhosts.conf
*** snap_install_old/conf/original/extra/httpd-vhosts.conf	2016-01-14 14:07:33.462452957 +0000
--- snap_install/conf/original/extra/httpd-vhosts.conf	2016-01-14 14:07:53.966452677 +0000
***************
*** 22,28 ****
  #
  <VirtualHost *:80>
      ServerAdmin webmaster@dummy-host.example.com
!     DocumentRoot "/CUSTOM_PREFIX/docs/dummy-host.example.com"
      ServerName dummy-host.example.com
      ServerAlias www.dummy-host.example.com
      ErrorLog "logs/dummy-host.example.com-error_log"
--- 22,28 ----
  #
  <VirtualHost *:80>
      ServerAdmin webmaster@dummy-host.example.com
!     DocumentRoot "${SNAP}/docs/dummy-host.example.com"
      ServerName dummy-host.example.com
      ServerAlias www.dummy-host.example.com
      ErrorLog "logs/dummy-host.example.com-error_log"
***************
*** 31,37 ****
  
  <VirtualHost *:80>
      ServerAdmin webmaster@dummy-host2.example.com
!     DocumentRoot "/CUSTOM_PREFIX/docs/dummy-host2.example.com"
      ServerName dummy-host2.example.com
      ErrorLog "logs/dummy-host2.example.com-error_log"
      CustomLog "logs/dummy-host2.example.com-access_log" common
--- 31,37 ----
  
  <VirtualHost *:80>
      ServerAdmin webmaster@dummy-host2.example.com
!     DocumentRoot "${SNAP}/docs/dummy-host2.example.com"
      ServerName dummy-host2.example.com
      ErrorLog "logs/dummy-host2.example.com-error_log"
      CustomLog "logs/dummy-host2.example.com-access_log" common
diff -rc snap_install_old/conf/original/httpd.conf snap_install/conf/original/httpd.conf
*** snap_install_old/conf/original/httpd.conf	2016-01-14 14:07:33.458452957 +0000
--- snap_install/conf/original/httpd.conf	2016-01-14 14:07:53.966452677 +0000
***************
*** 28,34 ****
  # same ServerRoot for multiple httpd daemons, you will need to change at
  # least PidFile.
  #
! ServerRoot "/CUSTOM_PREFIX"
  
  #
  # Mutex: Allows you to set the mutex mechanism and mutex file directory
--- 28,34 ----
  # same ServerRoot for multiple httpd daemons, you will need to change at
  # least PidFile.
  #
! ServerRoot "${SNAP}"
  
  #
  # Mutex: Allows you to set the mutex mechanism and mutex file directory
***************
*** 212,219 ****
  # documents. By default, all requests are taken from this directory, but
  # symbolic links and aliases may be used to point to other locations.
  #
! DocumentRoot "/CUSTOM_PREFIX/htdocs"
! <Directory "/CUSTOM_PREFIX/htdocs">
      #
      # Possible values for the Options directive are "None", "All",
      # or any combination of:
--- 212,219 ----
  # documents. By default, all requests are taken from this directory, but
  # symbolic links and aliases may be used to point to other locations.
  #
! DocumentRoot "${SNAP}/htdocs"
! <Directory "${SNAP}/htdocs">
      #
      # Possible values for the Options directive are "None", "All",
      # or any combination of:
***************
*** 329,335 ****
      # client.  The same rules about trailing "/" apply to ScriptAlias
      # directives as to Alias.
      #
!     ScriptAlias /cgi-bin/ "/CUSTOM_PREFIX/cgi-bin/"
  
  </IfModule>
  
--- 329,335 ----
      # client.  The same rules about trailing "/" apply to ScriptAlias
      # directives as to Alias.
      #
!     ScriptAlias /cgi-bin/ "${SNAP}/cgi-bin/"
  
  </IfModule>
  
***************
*** 342,351 ****
  </IfModule>
  
  #
! # "/CUSTOM_PREFIX/cgi-bin" should be changed to whatever your ScriptAliased
  # CGI directory exists, if you have that configured.
  #
! <Directory "/CUSTOM_PREFIX/cgi-bin">
      AllowOverride None
      Options None
      Require all granted
--- 342,351 ----
  </IfModule>
  
  #
! # "${SNAP}/cgi-bin" should be changed to whatever your ScriptAliased
  # CGI directory exists, if you have that configured.
  #
! <Directory "${SNAP}/cgi-bin">
      AllowOverride None
      Options None
      Require all granted
diff -rc snap_install_old/include/ap_config_auto.h snap_install/include/ap_config_auto.h
*** snap_install_old/include/ap_config_auto.h	2016-01-14 14:07:33.494452956 +0000
--- snap_install/include/ap_config_auto.h	2016-01-14 14:07:53.966452677 +0000
***************
*** 223,229 ****
  #define HAVE_UNISTD_H 1
  
  /* Root directory of the Apache install area */
! #define HTTPD_ROOT "/CUSTOM_PREFIX"
  
  /* Define to the address where bug reports for this package should be sent. */
  #define PACKAGE_BUGREPORT ""
--- 223,229 ----
  #define HAVE_UNISTD_H 1
  
  /* Root directory of the Apache install area */
! #define HTTPD_ROOT "${SNAP}"
  
  /* Define to the address where bug reports for this package should be sent. */
  #define PACKAGE_BUGREPORT ""
diff -rc snap_install_old/include/ap_config_layout.h snap_install/include/ap_config_layout.h
*** snap_install_old/include/ap_config_layout.h	2016-01-14 14:07:33.494452956 +0000
--- snap_install/include/ap_config_layout.h	2016-01-14 14:07:53.966452677 +0000
***************
*** 23,64 ****
  #define AP_CONFIG_LAYOUT_H
  
  /* Configured Apache directory layout */
! #define DEFAULT_PREFIX "/CUSTOM_PREFIX"
! #define DEFAULT_EXP_EXEC_PREFIX "/CUSTOM_PREFIX"
  #define DEFAULT_REL_EXEC_PREFIX ""
! #define DEFAULT_EXP_BINDIR "/CUSTOM_PREFIX/bin"
  #define DEFAULT_REL_BINDIR "bin"
! #define DEFAULT_EXP_SBINDIR "/CUSTOM_PREFIX/bin"
  #define DEFAULT_REL_SBINDIR "bin"
! #define DEFAULT_EXP_LIBEXECDIR "/CUSTOM_PREFIX/modules"
  #define DEFAULT_REL_LIBEXECDIR "modules"
! #define DEFAULT_EXP_MANDIR "/CUSTOM_PREFIX/man"
  #define DEFAULT_REL_MANDIR "man"
! #define DEFAULT_EXP_SYSCONFDIR "/CUSTOM_PREFIX/conf"
  #define DEFAULT_REL_SYSCONFDIR "conf"
! #define DEFAULT_EXP_DATADIR "/CUSTOM_PREFIX"
  #define DEFAULT_REL_DATADIR ""
! #define DEFAULT_EXP_INSTALLBUILDDIR "/CUSTOM_PREFIX/build"
  #define DEFAULT_REL_INSTALLBUILDDIR "build"
! #define DEFAULT_EXP_ERRORDIR "/CUSTOM_PREFIX/error"
  #define DEFAULT_REL_ERRORDIR "error"
! #define DEFAULT_EXP_ICONSDIR "/CUSTOM_PREFIX/icons"
  #define DEFAULT_REL_ICONSDIR "icons"
! #define DEFAULT_EXP_HTDOCSDIR "/CUSTOM_PREFIX/htdocs"
  #define DEFAULT_REL_HTDOCSDIR "htdocs"
! #define DEFAULT_EXP_MANUALDIR "/CUSTOM_PREFIX/manual"
  #define DEFAULT_REL_MANUALDIR "manual"
! #define DEFAULT_EXP_CGIDIR "/CUSTOM_PREFIX/cgi-bin"
  #define DEFAULT_REL_CGIDIR "cgi-bin"
! #define DEFAULT_EXP_INCLUDEDIR "/CUSTOM_PREFIX/include"
  #define DEFAULT_REL_INCLUDEDIR "include"
! #define DEFAULT_EXP_LOCALSTATEDIR "/CUSTOM_PREFIX"
  #define DEFAULT_REL_LOCALSTATEDIR ""
! #define DEFAULT_EXP_RUNTIMEDIR "/CUSTOM_PREFIX/logs"
  #define DEFAULT_REL_RUNTIMEDIR "logs"
! #define DEFAULT_EXP_LOGFILEDIR "/CUSTOM_PREFIX/logs"
  #define DEFAULT_REL_LOGFILEDIR "logs"
! #define DEFAULT_EXP_PROXYCACHEDIR "/CUSTOM_PREFIX/proxy"
  #define DEFAULT_REL_PROXYCACHEDIR "proxy"
  
  #endif /* AP_CONFIG_LAYOUT_H */
--- 23,64 ----
  #define AP_CONFIG_LAYOUT_H
  
  /* Configured Apache directory layout */
! #define DEFAULT_PREFIX "${SNAP}"
! #define DEFAULT_EXP_EXEC_PREFIX "${SNAP}"
  #define DEFAULT_REL_EXEC_PREFIX ""
! #define DEFAULT_EXP_BINDIR "${SNAP}/bin"
  #define DEFAULT_REL_BINDIR "bin"
! #define DEFAULT_EXP_SBINDIR "${SNAP}/bin"
  #define DEFAULT_REL_SBINDIR "bin"
! #define DEFAULT_EXP_LIBEXECDIR "${SNAP}/modules"
  #define DEFAULT_REL_LIBEXECDIR "modules"
! #define DEFAULT_EXP_MANDIR "${SNAP}/man"
  #define DEFAULT_REL_MANDIR "man"
! #define DEFAULT_EXP_SYSCONFDIR "${SNAP}/conf"
  #define DEFAULT_REL_SYSCONFDIR "conf"
! #define DEFAULT_EXP_DATADIR "${SNAP}"
  #define DEFAULT_REL_DATADIR ""
! #define DEFAULT_EXP_INSTALLBUILDDIR "${SNAP}/build"
  #define DEFAULT_REL_INSTALLBUILDDIR "build"
! #define DEFAULT_EXP_ERRORDIR "${SNAP}/error"
  #define DEFAULT_REL_ERRORDIR "error"
! #define DEFAULT_EXP_ICONSDIR "${SNAP}/icons"
  #define DEFAULT_REL_ICONSDIR "icons"
! #define DEFAULT_EXP_HTDOCSDIR "${SNAP}/htdocs"
  #define DEFAULT_REL_HTDOCSDIR "htdocs"
! #define DEFAULT_EXP_MANUALDIR "${SNAP}/manual"
  #define DEFAULT_REL_MANUALDIR "manual"
! #define DEFAULT_EXP_CGIDIR "${SNAP}/cgi-bin"
  #define DEFAULT_REL_CGIDIR "cgi-bin"
! #define DEFAULT_EXP_INCLUDEDIR "${SNAP}/include"
  #define DEFAULT_REL_INCLUDEDIR "include"
! #define DEFAULT_EXP_LOCALSTATEDIR "${SNAP}"
  #define DEFAULT_REL_LOCALSTATEDIR ""
! #define DEFAULT_EXP_RUNTIMEDIR "${SNAP}/logs"
  #define DEFAULT_REL_RUNTIMEDIR "logs"
! #define DEFAULT_EXP_LOGFILEDIR "${SNAP}/logs"
  #define DEFAULT_REL_LOGFILEDIR "logs"
! #define DEFAULT_EXP_PROXYCACHEDIR "${SNAP}/proxy"
  #define DEFAULT_REL_PROXYCACHEDIR "proxy"
  
  #endif /* AP_CONFIG_LAYOUT_H */
